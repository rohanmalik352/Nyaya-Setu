<% layout('layouts/boilerplate') %>

<body>

  <div class="ns-amb"
    style="background: radial-gradient(circle at 50% 50%, rgba(50, 210, 130, .08) 0%, transparent 60%);"></div>
  <div class="ns-stars" id="starsContainer"></div>

  <div class="ns-page">
    <header class="ns-hdr">
      <a href="/" class="ns-btn-sm"
        style="text-decoration: none; color: inherit; border-color: rgba(255,255,255,0.2);">← HOME</a>
      <div class="ns-logo-n" style="color: #34d399;">Judicial Chamber</div>
      <div style="width: 50px;"></div>
    </header>

    <div class="ns-db-out">
      <div class="ns-db-in ns-db-in--g">
        <div style="text-align: center; margin-bottom: 2rem;">
          <svg width="60" height="60" viewBox="0 0 32 32" fill="none">
            <circle cx="16" cy="16" r="14" stroke="#34d399" stroke-width="1.5" fill="rgba(50,210,130,0.1)" />
            <path d="M8 10 L5 18 H11 Z" fill="#34d399" opacity="0.8" />
            <path d="M24 10 L21 18 H27 Z" fill="#34d399" opacity="0.8" />
            <line x1="16" y1="6" x2="16" y2="24" stroke="#34d399" stroke-width="1.5" />
            <line x1="8" y1="10" x2="24" y2="10" stroke="#34d399" stroke-width="1.5" />
            <line x1="12" y1="24" x2="20" y2="24" stroke="#34d399" stroke-width="1.5" />
          </svg>
          <h2 class="ns-ct" style="color: #34d399; margin-top: 10px;">Evidentiary Integrity Check</h2>
        </div>

        <form action="/verify<%= (typeof preFetched !== 'undefined' && preFetched) ? '/' + preFetched._id : '' %>"
          method="POST" enctype="multipart/form-data">
          <input type="text" class="ns-fi ns-fi--g" name="caseId" id="caseIdInput" placeholder="Enter Case ID Reference"
            value="<%= (typeof preFetched !== 'undefined' && preFetched) ? preFetched.caseId : '' %>"
            <%= (typeof preFetched !== 'undefined' && preFetched) ? 'readonly' : 'required' %>
          >

          <!-- FILE UPLOAD: now accepts multiple files -->
          <label class="ns-fw" style="border-color: rgba(50, 210, 130, .4);">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#34d399" stroke-width="2">
              <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
              <polyline points="14 2 14 8 20 8" />
            </svg>
            <span class="ns-fw-lb" style="color: #34d399;">Upload File(s) to Verify</span>
            <span class="ns-fw-st" id="fileLabel" style="margin-left: auto;">No files selected</span>
            <input type="file" name="files" id="fileInput" multiple required>
          </label>

          <button type="submit" class="ns-sub ns-sub--g">Execute Verification</button>
        </form>

        <!-- RESULTS: renders one card per file -->
        <% if (typeof results !== 'undefined' && results && results.length > 0) { %>
          <div style="margin-top: 2rem;">
            <h3 style="color: #34d399; margin-bottom: 1rem; font-size: 0.85rem; letter-spacing: 0.1em; text-transform: uppercase;">
              Verification Results — <%= results.length %> File<%= results.length > 1 ? 's' : '' %>
            </h3>

            <% results.forEach((result, index) => { %>
              <div class="ns-stb <%= result.valid ? 'success' : 'error' %>"
                style="margin-bottom: 1rem; text-align: left;">

                <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.5rem;">
                  <span style="font-weight: bold;">
                    <%= result.valid ? '✓' : '⚠' %> <%= result.fileName %>
                  </span>
                  <span style="font-size: 0.75rem; opacity: 0.8;">
                    <%= result.valid ? 'INTEGRITY CONFIRMED' : 'TAMPER DETECTED' %>
                  </span>
                </div>

                <div style="font-family: monospace; font-size: 0.72rem; margin-top: 8px; opacity: 0.8;">
                  <div>📂 File Hash: <%= result.currentHash.substring(0, 25) %>...</div>
                  <% if (result.originalHash) { %>
                    <div>🗄️ DB Hash: &nbsp;<%= result.originalHash.substring(0, 25) %>...</div>
                  <% } %>
                  <% if (result.error) { %>
                    <div style="color: #f87171; margin-top: 4px;">⚠ <%= result.error %></div>
                  <% } %>
                </div>

                <!-- Blockchain status box per file -->
                <div id="blockchainStatus_<%= index %>" class="ns-stb loading"
                  style="display:none; margin-top: 0.75rem; font-size: 0.8rem;"></div>

                <!-- Golden Copy link if tampered and we have a DB record -->
                <% if (!result.valid && result.evidenceId) { %>
                  <a href="/evidence/retrieve/<%= result.evidenceId %>"
                    class="ns-sub--gold"
                    style="display: inline-block; margin-top: 0.75rem; font-size: 0.8rem;">
                    🔒 Fetch Original Evidence
                  </a>
                <% } %>

              </div>
            <% }) %>
          </div>
        <% } %>

        <!-- Legacy single result support (if result instead of results is passed) -->
        <% if (typeof result !== 'undefined' && result && (typeof results === 'undefined' || !results)) { %>
          <div class="ns-stb <%= result.valid ? 'success' : 'error' %>" style="margin-top: 2rem;">
            <%= result.valid ? '✓ INTEGRITY CONFIRMED: File Matches Database Record' : '⚠ WARNING: TAMPER DETECTED' %>
            <div style="font-family: monospace; font-size: 0.75rem; margin-top: 10px; opacity: 0.8; text-align: left;">
              <div>📂 File Hash: <%= result.currentHash.substring(0,25) %>...</div>
              <% if(result.originalHash) { %>
                <div>🗄️ DB Hash: &nbsp;<%= result.originalHash.substring(0,25) %>...</div>
              <% } %>
            </div>
          </div>
          <div id="blockchainStatus" class="ns-stb loading" style="display:none; margin-top: 1rem;"></div>
          <% if (!result.valid && preFetched) { %>
            <a href="/evidence/retrieve/<%= preFetched._id %>" class="ns-sub--gold" style="margin-top: 2rem;">
              🔒 Fetch Original Evidence
            </a>
          <% } %>
        <% } %>

      </div>
    </div>
  </div>

  <script>
    // --- Show selected filenames in the label ---
    document.getElementById('fileInput').addEventListener('change', function () {
      const label = document.getElementById('fileLabel');
      if (this.files.length === 0) {
        label.textContent = 'No files selected';
      } else if (this.files.length === 1) {
        label.textContent = this.files[0].name;
      } else {
        label.textContent = `${this.files.length} files selected`;
      }
    });

    // --- Blockchain Verification ---
    const CONTRACT_ADDRESS = "0x7C9069A677807e4D168e3a7c40bFbe790288Ad18";
    const ABI = [
      { "inputs": [{ "internalType": "string", "name": "_caseId", "type": "string" }], "name": "verifyEvidence", "outputs": [{ "internalType": "string", "name": "", "type": "string" }], "stateMutability": "view", "type": "function" }
    ];

    <% if (typeof results !== 'undefined' && results && results.length > 0) { %>
      const verifyPayload = <%- JSON.stringify(results.map((r, i) => ({
        index: i,
        caseId: r.currentHash,
        currentHash: r.currentHash
      }))) %>;

      window.addEventListener('load', () => {
        verifyPayload.forEach(item => {
          if (item.currentHash) {
            verifyOnChain(item.index, item.currentHash, item.currentHash);
          }
        });
      });
    <% } %>

    <% if (typeof result !== 'undefined' && result && (typeof results === 'undefined' || !results)) { %>
      const fileHash = "<%= result.currentHash %>";
      window.addEventListener('load', () => verifyOnChain(null, fileHash, fileHash));
    <% } %>

    async function verifyOnChain(index, caseId, currentFileHash) {
      const boxId = index !== null ? `blockchainStatus_${index}` : 'blockchainStatus';
      const box = document.getElementById(boxId);
      if (!box) return;

      box.style.display = 'block';
      box.textContent = "🔗 Connecting to Sepolia Blockchain...";

      if (!window.ethereum) {
        box.className = "ns-stb error";
        box.textContent = "⚠ MetaMask required for Blockchain Verification.";
        return;
      }

      try {
        const provider = new ethers.providers.Web3Provider(window.ethereum);
        const contract = new ethers.Contract(CONTRACT_ADDRESS, ABI, provider);

        const onChainHash = await contract.verifyEvidence(currentFileHash);

        if (!onChainHash || onChainHash === "") {
          box.className = "ns-stb error";
          box.textContent = "❌ No Blockchain Record Found for this file.";
          return;
        }

        if (onChainHash === currentFileHash) {
          box.className = "ns-stb success";
          box.innerHTML = "🔒 <strong>CHECKED ON BLOCKCHAIN</strong>";
        } else {
          box.className = "ns-stb error";
          box.innerHTML = "🚨 <strong>BLOCKCHAIN MISMATCH</strong>";
        }

      } catch (err) {
        console.error(err);
        box.className = "ns-stb error";
        box.textContent = "Blockchain Error: " + err.message;
      }
    }
  </script>
</body>
